{% extends 'tease.twig' %}

{% block content %}
	<h2 class="c-post__title">
    <a class="c-post__title-link" href="{{post.link}}">
      {{ post.title }}
    </a>
  </h2>

  <div class="c-post__body">
    <p>{{ post.get_preview(25) }}</p>
  </div>

	{% if post.thumbnail.src %}
		<img src="{{ post.thumbnail.src }}" class="c-post__featured-image">
	{% endif %}
{% endblock %}
