{% extends "base.twig" %}

{% block content %}
  <div class="o-wrapper">

    <ul class="o-list-bare">
    	{% for post in posts %}
    		<li>{% include ['tease-'~post.post_type~'.twig', 'tease.twig'] %}</li>
    	{% endfor %}
    </ul>
  </div>
{% endblock %}
